# Dependencies
# 不带斜杠：并行 worktree 按 CLAUDE.md 用 `ln -s` 复用主仓依赖，那是符号链接不是目录，
# `node_modules/` 匹配不上，会以 untracked 冒出来等着被误提交。
node_modules
.pnpm-store/
.npm-cache/

# Build output
dist/
dist-electron/
dist-local/
release/
.tmp/
.turbo/
coverage/
test-results/
playwright-report/
*.tsbuildinfo
public/tailwind.generated.css
src/styles/tailwind.generated.css

# Environment and local secrets
.env
.env.*

# Runtime data
project-data/
.data/
**/.data/
.agents/
.claude/
.tapcanvas/
artifacts/
orchestration/

# OS / editor
.DS_Store
*.log
*.out
*.err
.vscode/

# Temporary release and design work
temp-web-design-skill/
nomi-landing/
landing-page-*.html

.tmp
dist-electron

# Onboarding lab — local secrets + per-trial outputs
.secrets/
docs/onboarding-trials/20*/

# UX 走查截图产物（规则13）—— 不入库
tests/ux/shots/
# Playwright MCP 的 console 日志落点（探针跑完会留一堆，不入库）
.playwright-mcp/
tests/ux/__shots__/
tests/ux/_stagingshot/
tests/ux/_marketing/
tests/system/runs/
.scene3d-ux-lab/
.scene3d-panorama-lab/
.scene3d-stamp-lab/
.scene3d-keyframe-lab/
.scene3d-safeframe-lab/
.viewfinder-marker-lab/
.onboarding-demo-lab/
.video-playback-heal-lab/
.library-cover-walk-lab/
.project-asset-healthcheck-lab/
.toolbar-order-lab/
.text-error-lab/
# nomi-local 协议走查/压测的 fixture 与产出（里面躺着 ~500MB 的 mp4，一次 `git add -A` 就能进库）
.local-protocol-seek-walk/
.local-protocol-abort-stress/

# 3D 预设动作校准台的本地输出（pose-lab）
.pose-lab/
.pose-lab-vite.log

# 浏览器自动化测试 profile 缓存（含 Service Worker 缓存里的第三方 key，绝不入库）
.independent-retest/

.tmp/

# 下载量可视化看板：派生产物（pnpm stats:html 按需生成），数据每次都变故不提交
docs/stats/dashboard.html

# ── Feedback Radar 用户私有数据（微信聊天记录/评论/昵称/db_key）· 安全系统见 docs/security/ ──
# whitelist 模式：docs/feedback/ 默认全部 ignore，只放行模板与说明（防"一不留神"新数据文件进 git）
docs/feedback/*
!docs/feedback/sources.example.json
!docs/feedback/README.md
# 微信取钥产物 / WeLive 配置 / 数据库文件——任何位置都不许进 git
**/welive.yaml
**/welive.yml
**/all_keys.json
**/keys.json
**/wechat_keys.json
**/wechat-export/
*.db

# R13 走查截图产物（scripts/*-walkthrough.mjs 输出，纯本地证据，不进 git）
# 通用模式覆盖所有 .{name}-walk/ 输出目录；不合模式的特例单列
.*-walk/
.comfyui-real-user-journey/
.failure-walk-ok/
.model-panel-shot/
.custom-call-recon/
.model-home-mockup-review/
.settings-original-main-baseline/

# Python 运行副产物（decrypt_wechat.py 等被 import/执行时生成，绝不入库）
__pycache__/
*.pyc

# 走查产出的截图（可随时重跑生成，别进库）
docs/design/mockups/2026-08-20-grid-split-freeze/

# design-sync (component library sync) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
# 由 .design-sync/support/build-css.mjs 生成（~620KB 的样式压平产物）：
# 生成脚本进库、产物不进库。改了样式源码后重跑那个脚本即可。
.design-sync/support/styles.generated.css
