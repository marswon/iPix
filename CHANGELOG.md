# 更新日志

本项目的所有重要变更记录于此。版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## v0.21.0 — ComfyUI 多输入、2026 现役模型与多镜制作闭环（2026-08-27）

> 这次更新把多张参考图、视频和音频送进真正独立的模型槽位，并把 Wan 3.0、MiniMax H3、火山方舟新模型与 Gemini/Antigravity 接入同一套画布制作流程；生成前确认、预算停止、锚点检查、返工续拍与版本采纳组成更完整的多镜生产链。

### ComfyUI 多输入工作流

- 图片/视频输入按工作流参数键保留独立身份，覆盖上传、画布连线、pending、保存、冷启动恢复和最终请求，不再被通用参考图别名串位。
- 支持多图和图 + 视频工作流，按实际 saver 判断输出类型；长枚举搜索、光标锚定缩放和 Mac 触控板偏好同步完善。

### 新模型与渠道

- 接入 Wan 3.0：KIE 标准/高速与 APIMart，覆盖文生、首帧、首尾帧、全能参考四种模式。
- 接入 MiniMax H3 的 KIE/APIMart 多模式、Context-IR 提示词增强和 APIMart 2K 再生成。
- 修复火山方舟原生端点、供应商归一和模式参数切换；新增 Seedream 5.0 Pro、Seedance 2.5。
- 新增 Nano Banana 2、Seedream 5、Qwen Image 3、Flux 2 Pro 等 2026 生图档案。
- 增加 Gemini 文本/视觉路由、Agnes 完整模型合同与 Antigravity 本地文本/视觉/生图/改图能力。

### 多镜制作与采纳

- 增加逐镜真实价格、计划确认、批次调度、预算/手动停止、形象检查点、暂停恢复和镜头返工版本链。
- 多镜语义任务进入真实 provider create；生成结果通过唯一采纳桥进入时间轴，避免多入口写入冲突。
- 用户界面把内部“合同/锚”术语改成面向创作者的计划、参考形象与确认表达。

### 画布、素材与桌面可靠性

- 素材库支持本地图片粘贴/拖入；修复 data URI、base64、媒体扩展名、本地持久化和主动视频播放声音。
- 修复窄窗口生成卡布局、动画取消/重入与 React StrictMode 手势失效；模型发现与选择恢复更稳。
- MCP/IPC 加固 owner、sender、取消、并发付费确认和 schema；Electron 升级到 43.4.1，并增加 Windows 与 Mac 双架构验证。

### 兼容性与边界

- macOS 最低版本提高到 macOS 12；macOS 10.15 / 11 请继续使用 v0.20.1。
- macOS 仍未签名/未公证，Windows 未使用 Authenticode。
- ComfyUI 私有 GPU 画质、部分受账号资格限制的火山/Gemini 能力不作普遍可用承诺。
- 非硬切转场仍只保存元数据，导出尚未渲染视觉 xfade。

## 协议变更 — 开源协议切换为 AGPL-3.0（2026-08-01）

- Nomi 的开源协议由 Apache-2.0 切换为 **AGPL-3.0**（自本次提交起生效；已发布的历史版本 ≤v0.18.1 仍按 Apache-2.0 授权）。
- 对个人用户与公司内部使用**没有任何影响**；变化只针对「把 Nomi 修改后闭源分发 / 作为在线服务提供」——这类使用现在必须同样开源，或联系作者获取商业授权（微信 TZ857886159）。
- 外部贡献自即日起合并前需一次性签署 [CLA](CLA.md)（PR 内机器人自动引导）。

## v0.20.1 — 画布反馈修复、低缩放媒体预览与 Agent 生产链收口（2026-08-22）

> 本版本基于 v0.20.0 之后的主线迭代，优先收口最近用户反馈：让画布菜单真的点得动、素材多时仍看得见预览、九宫格切图不再卡死，并把外部 Agent 从“能调用”推进到“能审片、能恢复的制作链”。

### 画布与媒体

- 右键空白和右键节点都有可发现、可点击的菜单；复制、粘贴、撤销、删除同时覆盖 Cmd 与 Ctrl，并补上真实 Electron 走查。
- 大画布低缩放时继续显示图片/视频缩略图；修复本地媒体流重复包装导致的异常窗口。
- 九宫格/四视图切图改为 Blob 落盘，避免大段 base64 进入状态和事件日志；切片逐步落成独立节点、自动编组并自动适配视口，一次切图可一步撤销。
- 导入的工作流参数显示可理解的参数标签；画布复制和版本入口更容易找到。

### Agent、MCP 与生产运行

- MCP 工具扩展到 22 个，支持 brief 收敛、本地素材导入、分镜物化、结果缩略图/深链、会话信任和批次闸门。
- 增加首尾帧锚定、定妆冻结、镜头语言字段化，以及审片评分 → 定向重试 → 红标交付链路。
- Production Run 补齐草稿片从剧本、分镜、画布、时间轴、字幕到 MP4 的可恢复合同；本地视频资产传输和重启恢复更可靠。

### 创作与模型接入

- 创作助手增加 Prompt 选择器、自定义 Prompt 和系统提示词设置；分镜支持批量修改类型、模型和时长。
- 新建节点默认模型选择更稳定，修复中文输入 `@` 引用参考图，并刷新 DeepSeek 文本模型档案。

### 诚实边界

- 本版本的 draft-film 合同验收使用仓库已有 launch film 素材，证明的是生产、字幕、转场元数据和导出合同，不代表新模型生成质量。
- 当前导出器对 `dissolve`、`fade` 等非硬切仍只保留元数据，尚未渲染成视觉 xfade。
- 正式安装包仍只发布 macOS arm64、macOS Intel 和 Windows x64；macOS 未签名/未公证，Windows 未使用 Authenticode。

## v0.20.0 — Agent 生产闭环、模型接入重构与剪辑画布强化（2026-08-17）

> 本版本汇总 v0.19.0 之后 185 个主线提交，重点把“接入模型、让外部 Agent 可靠执行、在画布完成生成与剪辑、拿到可安装更新”四条链路连成闭环。

### Agent 生产与 MCP

- 生产运行升级为可恢复的持久任务：创意方向、首镜抽检、预算与信任等级都有明确关卡，并支持暂停、恢复和取消。
- MCP 返回改成面向对话的状态与错误契约，补齐真实服务旅程、跨平台打包启动器识别和宿主激活测试。
- 批量生成、后台通知、任务恢复和运行控制统一到同一套生产状态，不再依赖临时 UI 状态猜进度。

### 模型接入与生成

- 模型管理集中到设置页，保存优先、失败可恢复；自建端点、局域网端点、无 Key 服务和中转模型按真实能力验证。
- 自定义调用命名配置迁入系统安全存储；无 Key 的 OpenAI-compatible 端点不再发送空的 `Authorization` 请求头。
- 新增 APIMart Seedance 2.5、Gemini 3.5 Flash 等模型档案，并补齐自定义调用参数、真实来源门岗和首尾帧/多参考约束。
- ComfyUI 通用工作流核心与真实用户旅程加固，模型选择、参数桥接、任务结果解析和可用性判断更稳定。

### 画布、剪辑与媒体

- Clip 节点补齐拆分、裁剪、删除、插入和可靠拖拽；时间轴按真实内容增长，播放、首帧与媒体加载路径统一。
- 大画布调度只保留可见媒体的可渲染性，降低密集项目中的资源占用，同时避免滚动后可见内容空白。
- 节点浮动工具、边线、图片编辑、复制粘贴和布局交互完成一轮一致性与性能修复。

### 桌面可靠性与发布

- 修复关闭窗口后无法重开、保存对话框跨线程崩溃、浏览器菜单销毁竞态和损坏 stdio 导致的进程退出。
- 新增隔离的 Desktop Preview、不可变 RC 与正式晋级流程；正式 Release 复用已验收 RC 产物，不在发布时重新构建。
- 官网可按 macOS Apple Silicon、Intel 和 Windows x64 直接下载稳定安装包；应用内更新会跳转到正确架构的官网入口。

## v0.19.0 — 画布语义重构、ComfyUI 接入大修、技能/MCP 体系与全量反馈修复（2026-08-08）

> 本版本是 v0.18.1 之后约 200 次提交的首个发布，集中消化了飞书反馈修复包、画布交互重构、ComfyUI 本地生图大修、技能库/MCP 体系、时间轴/剪辑体验、3D 导演台、设置集中化与营销站双语化等一整轮积累。

### 🖱️ 画布操作语义重构（selection-first）
- 画布操作语义翻转：空白拖=平移、滚轮=光标锚定缩放、Shift+拖=框选，空格拖恢复平移（补齐需求池 #832）。
- 选中成为首要手势：浮条「选中才出」、多选删除带「撤销」安全网、组端口 + 整组运行（一根线喂满一整场戏）。
- 控件层级梳理：全 App 控件层级梳理 + 顶栏右簇 7 个平铺 → 分 3 组（语言/外观/关于归位设置）。
- @ 能艾特到画布和素材库且艾特即真连上；全局截图进画布热键；成图卡片右上角按钮搬进 hover 浮条、角色/场景/道具卡「上传图」修复。

### 🖥️ ComfyUI 本地生图接入大修
- 工作流识别率 41%→86%（259 张真实模板实测，三根因根治）。
- 多实例 UI + 后端解耦：每台一张卡，选模型即选机器；combo 参数烤成真实文件下拉（checkpoint/LoRA 不再手抄）。
- 视频输入通道打通（补帧/超分/去背景）；Tier-2 双轨 ws 实时进度/活预览/遮罩取消。
- 导入缺件对账（`/object_info`）+ 首跑必炸根治 + `/prompt` 400 人话；贴什么格式都吃 + 493 个官方模板；ComfyUI 0.30 子图工作流识别。

### 🎬 技能库 / MCP Apps / 阿泽导演方法论
- 剧本系统 11 个编剧技能、导演技能第二批（表演/调度/服化道/审图/声音/风格/古装）整入。
- 集成阿泽导演方法论：拆镜头演时换算 + 5 个导演技能；运镜翻译表折入拆镜头（应用内规划师写得出专业运镜）。
- MCP Apps：技能库挂上 MCP 脊柱（resources + prompts）、Phase C 活生成 widget（GUI 宿主内嵌活面板）、跨宿主就绪（ChatGPT 双桥 + always 广告）。
- 外部 agent 经 MCP 真驱动 Nomi 出真图（真 stdio 服务验 23 技能闭环）；自定义调用脚本让接不通的中转模型当场被教会调。

### ⏱️ 时间轴 / 剪辑页体验
- 时间轴体验包：全员真帧胶片 + 点击贴尾入轨 + 拖放滑入不拒收 + 幽灵播放头 + 点哪落哪 + 空隙如实文案 + 画面小窗。
- 剪辑页左侧素材来源栏（画布镜头 + 项目素材含配乐），可折叠；空态「一键拼成初稿」提示行。
- clip 宽度/切点范围改读可见跨度（治裁剪 UI 纹丝不动）；小窗圆角引用不存在 token 的渲染 bug 修复。

### 🧊 3D 导演台 / 场景
- 轨迹「新建」后右栏属性面板即时激活（不用再去时间轴点一次）。
- UE 人偶 +20 姿势收编；道具库扩容 11 种（交通/家居/街景灰模）。
- WASD 走位假人朝向根因修复（正面 +Z 约定，原算成相机 -Z）。

### ⚙️ 设置页集中 + 网络代理
- 集中设置页：UI/入口/接线/走查全链；studio 工作区顶栏也加设置齿轮入口。
- 网络行：给 Nomi 单独设代理，SOCKS 代理真支持（undici 6 自接 socks connector），状态一眼可见。
- 项目位置：可选新项目落点并持久化，跨存储变更保留项目来源。

### 📚 素材库 / 资产管理
- 参考图 hover 自动放大、素材库双击放大预览、项目库列表双击改名。
- 自动另存后端地基；生成结果绝不再裸存易失 CDN url（隔天视频/图片加载不出根治）。
- 免配置图床有效期 1h → 24h；下载改保存名不再闪退（Windows 文件名加固）。
- 同名 modelKey 跨厂商不再互吞/翻家；中转接入模型身份认错修复（13/31 认不回档案）；渠道承载力判据 UI 收窄与第三闸共用。

### 📋 任务中心外化
- 生成队列外化 + 任务面板 + 失焦提醒；批量任务入口常驻可见。

### 🐛 飞书反馈修复包（八连修 + 二轮）
- 八连修：遮挡/按钮/落点/布局根因各归各位。
- 二轮：素材库拖宽、快捷键溢出、时间轴居中、胶囊挪位、模型浮卡去重标题与状态语义。
- 工作区 19 项修复包：拖拽/参数类型/连通性/下载追踪/打包体检等落地。

### 🌐 营销站 / README 双语化
- 双语静态首页生成 + 双语上线 + 商务入口；社区/直营转化路径保护与走查锁定。
- 微信转化前置首屏；移动端群二维码醒目可扫。

### 🔧 报错说人话 / 微信反馈工作流
- 微信反馈梳理工作流：图片进时间线 + 全量导出 + HTML 对账表，`decrypt_wechat` 加 `--limit` 全量回溯。
- 图床全挂先指向代理；内容安全拦截/素材上传失败不再压成「参数不被接受」；生图失败限流/额度原话透传（Codex 不再误标「未启用生图能力」）。
- 轮询按后端分档 + 抖动 + 429 指数退避；播放守卫从画布节点提到各播放面共用。

### ⚖️ 协议切换
- 开源协议 Apache-2.0 → AGPL-3.0 双轨（详见文件顶部「协议变更」说明）。

### 🧹 构建 / 清理 / 稳定性
- 根治孤儿进程 EIO 递归报错；清理 5 处已实证死码；pnpm store 回默认位置（多 worktree 不再各建冷 store）。
- tailwind 产物随改动重建；移除误提交的 node_modules 符号链接；外部贡献经 CLA 机器人引导合入（PR #56）。

### 🧭 本次收尾修复（工作台布局）
- **生成页 AI 胶囊回归右上角**：将「Nomi 生成」助手胶囊从画布右下角恢复回右上角固定位置（`top-4 right-4`），避免与时间轴、画布节点落点冲突。
- **画布快捷键帮助面板居中防遮挡**：将面板由右下锚点向左展开改为以触发按钮为中心水平居中（`left-1/2 -translate-x-1/2`），并维持 `w-[30rem]` 宽度，解决左侧项目栏遮挡与右侧越视口边界的两难问题。
- `canvasControlsStructure.test.ts` 同步更新布局断言，确保快捷键面板定位与实现一致。

## v0.18.1 — 中转站接入大修：Seedance 2.0 全模式打通（2026-07-31）

### 🔌 中转站接入：从「拉不到、连不上、参考图丢」到全模式可用
- **裸地址也能拉到模型**：new-api / one-api 系中转的后台网页不再骗过探测——地址带不带 `/v1` 都能自动拉到模型列表。
- **纯图片 / 视频中转不再误报「连不上」**：「测试连接」不再拿视频模型发聊天请求；没有文本模型时改验地址 + Key 可达性，并如实说明。
- **中转视频模型补上「图生视频」通道**：此前连首帧图必被拒发且提示误导；已接入的存量模型下次启动**自动自愈**，不用删了重加。
- **认得出的模型自动走厂商原生接口**：接入时自动探测中转是否代理厂商原生端点（如火山方舟），是则直接用官方原生报文——接 Seedance 2.0 的中转从此**首帧 / 尾帧 / 角色参考图×9 / 参考视频×3 / 参考音频×3 / 生成音频全模式可用**（对官方方舟真机逐模式实证）。
- **发不出去的参考不再静默丢**：连了参考素材但当前线路装不下时，扣费前拒发并说清是哪几类，绝不默默退化成纯文生。

### 🧾 报错说人话
- 拉模型 / 测连接 / 生成失败，界面直接显示**上游服务器原话**（不再整坨 JSON 或含糊「没拉到」）。
- **每类失败都说得出「该干嘛」**：确定性失败不再一律给「重试」——该换模型的给「换模型」，该查 Key 的指向 Key；连续失败的「病模型」自动沉底不再霸占默认位。
- 任务被上游**取消**（cancelled）立即报终态，不再傻等到超时。

### 🖼️ 其他修复
- 新手引导的示例成图落成真项目资产——换电脑 / 更新版本后不再裂图。
- 本地生图（ComfyUI 等）轮询按后端时延分档，不再被云端 2 分钟硬超时腰斩。
- 下载「另存」记住上次目录，下次默认弹到那里。
- Imagen 4 上游已失效，退役下架（存量自动清理）。

## v0.18.0 — 3D 导演台：出片这条链，终于像剪辑软件了（2026-07-30）

### 🎬 3D 导演台：从「摆完不知道怎么出片」到「一路顺到成片」
- **出片终点不再藏、缺步一键补**：顶栏切「运镜参考」就出「生成参考视频」；没绑相机就点它，不再点了没反应——直接弹「一键绑定相机并生成」，一下补上并出片。
- **时间轴点哪跳哪**：点标尺任意处 / 拖轨道，播放头就跟过去，不用再去够那颗小球。
- **主画面一键看成片**：左下「预览最终画面」→ 主视口切成相机镜头，拖时间轴实时刷成片；空格播放 / 暂停。
- **时间轴 = 成片长度**：应用运镜后时间轴显示的就是真实成片时长（5 秒的运镜不再撑成 10 秒）；删 / 缩内容，尺子跟着回缩，所见即所得。

### 🎨 更多模型可选
- apimart 新增 **Vidu Q3、可灵 3.0 Turbo、HappyHorse 1.1、Seedream 5.0 Pro、Wan2.7（角色参考）**。

### 🛡️ apimart 被墙也能用
- 主域在大陆被墙时，自动探测官方候选域、在连接层安全换线（换线只在免费的探测 / 连接层，绝不重复触发付费提交）；主域恢复后自动切回。

### ⚡ 批量更顺手
- 批量里失败的那几个**一键重试**；拆镜头结果落画布**自动全选**；一次要 N 个变体**连发**。
- 上游挂掉的模型不再霸占「默认」——默认会自动避让最近失败的模型。

### 🎞️ 图片 + 视频分镜
- 分镜可**混排图片镜头与视频镜头**；某镜的首帧图与它所属视频**共用镜号**，不再各算各的。

### 🧩 本地 ComfyUI 更完整
- 自定义 workflow 的**参数可调**；视频预览与时长探测修好，导出更稳。

### 🖼️ 导入 / 参考更稳
- **导入参考视频不再灰壳 / 0:00 不可播**：进口时自动归一化 codec + 播放守卫懒自愈。
- 挂了参考图却停在「文生图」时，参考**不再被静默丢弃**（模式 × 连线全入口对齐）。

### 😀 开箱即用的表情预设
- 内置「表情预设」包：**5 种情绪 × 5 档强度**的提示词 + 男女对照定妆图，点一下就用。

### 🔧 一堆体验修复
- 创作助手**默认常驻右侧栏**，收起也不丢状态。
- 全 App 的 toast **隐形问题根治**（此前 3D 编辑器 / 全景导入里的提示看不见）。
- relay 生图遇 403「分组未开通」**自动回退 chat 出图**；档案槽上传进标准参考面、不再被独占丢参考。
- 素材库 / 画板 **IA 归位**：画布成图归「结果」页签（根治群反馈「多出的图删不掉」）；画板节点「打开」入口找回。
- dev 版被 Apple 吊销公证导致启动即被杀 **自愈**；Codex 本地异步生图桥 + mac/linux 自动探安装位。

## v0.17.0 — Nomi 会说英文了 + 3D 导演台成片可信度大补（2026-07-23）

### 🌍 Nomi 会说英文了（中英双语）
- 全站中英文双语，首次启动**自动跟随系统语言**；顶栏一键切换 中文 / English，切一次后记住。
- 英文用户从此能完整用上 Nomi——此前只有中文界面。

### 🎬 3D 导演台：从「能出片」到「出的片能用」
- **默认 / 双人机位不再截头**：此前默认相机常把整颗头切在画框外；现在按人物真实包围盒 + 画幅自动安全取景，头脚都在画面里。切画幅（16:9 / 9:16 / 竖屏…）也重新安全取景；你手动调过机位后系统不再偷偷覆盖。
- **一次点击就开始录制**：「开始录制」点一下 → 3 秒倒计时 → 直接开录，不用再点第二次。
- **蹲下真的进成片**：录人物动作时按住 C 蹲下，现在会真正录进最终 MP4——此前只在现场显示、成片里还站着。
- **跟拍真的跟住人**：先选「右横移跟拍」再录人物动作，镜头会持续看住移动的人物——此前只剩右横移、人物漂出画面。
- **首尾帧有了完成卡**：导出运镜首尾帧后有持久结果卡 +「回画布查看」，不再只闪一下提示。
- 三任务入口（构图 / 人物动作 / 运镜参考）+ 完成按钮直达产物 + 主视图明说是「输出画面」还是「工作视图」。

### 🌐 网页素材：抓到什么，诚实告诉你
- 素材盒里**一眼可见的「放到画布」主动作**：抓到的图直接放画布，不用再翻右键菜单；成功后明说「已放到画布 · 关闭浏览器查看」。
- 抓视频当前帧时明说是「视频当前帧」；黑屏 / 纯色空帧会被拒绝（提示先播放到有效画面）；动图（GIF / 动画 WebP）标为「动态图」。
- 登录墙 / 人机验证给出可行动提示，不把广告帧 / 登录层背景当素材成功入库。

### 📦 素材面收敛：一个功能一个家
- 提示词只归主提示词库一处；托盘退回纯捕捞收件箱；文件夹转正进素材库；删除即真删。

### 🔌 接入更顺
- 通用图生图（GPT Image 等）支持 `/v1/images/edits` multipart，自动探测协议；存量模型自愈升级。
- ComfyUI 自定义 workflow 导入修复 + 支持云端地址（cnb.cool / cloudstudio.net）。
- 模型设置桥缺失不再无限「加载中…」，改为有界重试 + 可操作错误态。

### 🐛 其它修复
- 导入视频当参考不再被误判成图导致加载失败（按真实媒体类型判定）。
- 创作助手面板裁切（#46）、提示词编辑光标、连线交互等修复。

## v0.16.9 — 3D 导演台大改：以出片为锚点的用户旅程（2026-07-21）

### 🎬 整完运镜，终于有「出片」了
- 顶栏新增**「出片」主按钮**：参考视频（推荐）/ 截图 / 首尾帧三种产物一个出口。此前整完轨迹没有任何入口能导出 mp4（唯一出口藏在录 take 里），现在**已有轨迹直接出参考视频**，不用再录一遍。
- 出片时长自动裁到运动终点：此前按时间轴默认 10 秒渲染，3 秒运镜会带 7 秒定格尾巴喂给下游模型；现在 mp4 就是运镜本身的长度。
- **产物卡片全程陪跑**：渲染中 → ✅ 已生成（已建画布节点 · 是否已喂下游镜头）→ 一键「回画布查看」。截图也有完成卡——产物落在被编辑器盖住的画布上，此前只有一条一闪而过的提示，会以为什么都没发生。
- 运镜就绪自动接力提示：「运镜就绪 → 点顶部出片」。

### 🧭 界面重排：37 个一级控件收到 22 个，每个功能只有一个家
- **顶栏 10 个控件减到 4 个**（标题 · 出片 · 重看引导 · 关闭）：截图/播放/轨迹三个重复入口删除；移动/旋转落到视口左上角（贴着你操作的东西）；速度滑杆只在真用 WASD 的操控条里出现。
- **整运镜三合一**：预设 / 手动轨迹 / 录 take 三条路收进右栏常驻分区，不用再满界面找「怎么动镜头」。13 招运镜预设从属性面板最底部提到第一屏。
- **时间轴按时机出现**：进门先见「添加」，第一段运镜诞生那刻时间轴自动弹出——不再一进来就被空时间轴糊脸。
- 新手引导重写为 5 步旅程（摆场景→摆角色→摆相机→整运镜→出片），顶栏 ? 随时重看。

### 🛠 操控顺手了
- **拆掉「轨迹模式陷阱」**：此前从整运镜点过轨迹后，点画面里的人物/物体全无反应（被静默困在轨迹编辑态）；现在点任何对象=自动退出轨迹编辑+选中它。
- **进门看得见相机了**：出厂视角此前刚好把相机1排在画面外（很多人以为场景里没相机）；现在按场景内容自动取景，并新增视口「看全场」按钮，飞丢了一键回家。
- 报错带一键跳转：没选相机点截图→点提示直接帮你选中；没绑定点播放→点提示直接进轨迹面板。
- 删掉左下角无用的 XYZ 装饰徽标。

## v0.16.8 — 浏览器素材盒拖拽与下载链根治（2026-07-18）

### 🧰 网页参考图 / 参考视频终于能稳定进素材盒
- 网页素材改用**原页面 Session** 下载，完整保留登录 Cookie、Referer 和重定向；Dribbble 真实 CDN 图片已真机拖入通过，不再失败后偷偷换一套丢登录态的网络请求。
- 下载改为流式落盘并校验文件魔数：拒绝伪装成图片的 HTML 错误页，支持常见图片、MP4/WebM/MOV/M4V/OGV/AVI/MPEG 视频；文件扩展名永远跟真实内容一致。
- 失败卡显示“网站拒绝下载 / 返回的不是图片或视频 / 页面会话已失效”等真实原因，下载中和失败素材不再伪装成可拖。

### 🖱️ 并排 / 浮动不再吞网页点击
- 根治 macOS 不支持 `BrowserWindow.setShape()` 却把整块透明窗口设为可点的问题；素材盒外的网页保持可点击、可发起拖拽。
- 拖拽结束同时释放 drag / pointer 命中态，不再出现“拖完后中间点不动、点一下素材盒消失”。
- 新增 macOS CoreGraphics 原生回归旅程：**并排拖入 → 恢复浮动 → 再拖入 → 点击底层网页**，两次落库、网页点击计数和素材盒存活全部通过。

### 🧭 浏览器按钮与菜单回归
- 修复原生标签 / 截图菜单 preload 路径和 CSP，Escape 先关当前菜单，不会连浏览器一起关。
- 素材盒搜索、上传、文件夹、网格/列表、排序、筛选、右键、并排/恢复、收起/重开，以及浏览器前进/后退/刷新/书签/标签等均完成按钮级真机走查。

## v0.16.7 — ComfyUI 自定义工作流导入 + 拆镜头截断根治（2026-07-17）

### 🖥️ 本地 ComfyUI：不再只能用内置工作流，接你自己搭的任意流程
- 此前本地 ComfyUI 只有一个内置「文生图」。现在在 ComfyUI 里 **Export (API) 导出 `workflow_api.json` → 贴进 Nomi → 自动识别可绑定节点（提示词 / 首帧 / 输出 / 尺寸种子等数值）→ 下拉确认或微调 → 命名落库**，之后在生成画布直接选用你自己的工作流。群里问的「WAN2.2 接不进」到此为止。
- **视频工作流也能接**：读取 ComfyUI 的 `gifs`/`videos` 输出（WAN/VHS VideoCombine、原生 SaveVideo 全覆盖），不再只认图片。
- **图生视频首帧**：首帧图自动上传进 ComfyUI 的 input 目录（LoadImage 只认本地文件名，已打通）。
- 全程数据不出本地，用你自己的 GPU 和模型。

### ✂️ 拆镜头不再「内容被截断、还骗你重试」
- 拆镜头连续失败报「模型达到输出长度上限、内容被截断」，还误导「服务商临时故障、建议稍等重试」——但截断是确定性的，重试必再撞。**根治**：删掉代码里硬编造的 `max_tokens` 上限（它把整份拆镜头方案 JSON 拦腰截断；推理模型的思考 tokens 还额外占额度、更早撞线），改用模型自身默认 / 你在模型条目填的上限；截断错误改成**说真话 + 给真动作**（剧本分段拆、减少镜头数、换单轮输出更长的模型）。（社区 PR #39，感谢 @1251912798）

### 🎬 拆镜头总难触发 → 到时机自动把入口端到眼前
- 写好故事（≥60 字）、还没拆过镜头时，创作助手顶部**自动浮一张「拆成镜头」卡**（图片 / 视频二选一，点了才跑、可关）——不加常驻按钮、贴合对话驱动，只在对的时机把入口递到眼前。
- 放宽识别词（「规划镜头」「拆成场景」也认），仍不误伤「这个场景描写不错」。
- 「写分镜」模式改名「**写分镜文字稿**」——它只写文字稿、不落画布，旧名和画布「分镜」撞车害人误选。

### 🗂️ 素材盒文件夹重命名
- 素材盒里**右键单选文件夹 →「重命名」**原位改名，或**新建文件夹即进入重命名态**；Enter 提交、Esc 取消。（社区 PR #38，感谢 @1251912798）

### 🛠️ 修复
- **超长提示词溢出**：生成节点里写很长的提示词时，文字下溢盖住底栏「选择模型 / 优化 / 生成」、点不到——现在长文字在编辑区内滚动，底栏永远贴底可点。
- 浏览器 / 素材盒一批真机修复：标签页 favicon 裂图回退、捕捞素材显示哈希文件名改人类可读标题、素材盒设置框 / 工具条点不进去（窗口穿透根治）。

## v0.16.6 — 应用内浏览器 + 全局素材盒（2026-07-12）

### 🌐 Nomi 里长出一个真浏览器（社区贡献 @1251912798，PR #36）
- **顶栏「浏览器」**：多标签、书签收藏、素材网站快捷入口——找参考全程不用切出 Nomi。
- **素材盒**：素材收件箱。开着「资源捕捞」，网页里看中的图/视频**悬停按 Ctrl+C**、或**直接拖进盒子**，就落进当前项目素材库；顶栏徽章实时计数。
- **网页上「偷师」**：划词保存提示词；选区截图让 AI 提取提示词或复刻风格，直接进提示词库。
- **画布多选拖拽**：素材盒里多选素材，一把拖上画布。

### 🔁 「网页捕捞」并入同一引擎
- 素材库的「网页捕捞」按钮保留，点开的就是上面这个浏览器——原独立捕捞窗退役，登录态、书签、捕捞收进一个地方（旧捕捞窗里登录过的网站需重新登录一次）。
- 防盗链下载（走网页自身会话）、权限默认拒绝的安全基线原样保留。
- **隐私加固**：捕捞素材只存本地文件，**你浏览的网页地址绝不会被当作参考链接发给任何生成服务商**。

### 🎨 顶栏与左栏视觉统一
- 顶栏所有操作统一「图标+文字」（素材盒不再是孤零零一个图标）；左栏功能条图标下加微字标签（素材库/找素材/分类/提示词/技能），一眼认出不用悬停猜。

### 🛠 修复
- 素材捕捞/导入后，素材库面板与素材盒徽章即时刷新（此前要手动切换页签才出现）。
- 打包版每次启动 console 必报一条 CSP 错误的问题（清理内联脚本）。
- http 站点 favicon 触发报错、起始页站点图标依赖境外服务经常加载失败——改为本地首字母图标，零网络依赖。

## v0.16.5 — 3D 导演台首次进入引导（2026-07-11）

### 🎓 第一次进 3D 导演台，三步带你上手
- 首次打开 3D 编辑器自动播三步教练标注：**点假人**（右侧换姿势、头顶出「操控」进 WASD 走位）→ **点相机**（解锁画面预览与 13 招运镜预设）→ **底部「添加」**（城市街道/室内房间场景模板与道具）。可跳过，只出现一次——导演台三个藏得最深的能力不再靠试错发现。
- 新手旅途里 3D 站位/运镜两步聚光引导此前指向失效（锚点缺失），已修复指向 3D 节点。

### 🧍 随 v0.16.4 的假人姿势校准与网页素材捕捞一并打包（网盘用户直接装本版即可）。

## v0.16.4 — 网页素材捕捞 + 假人姿势人眼级校准（2026-07-11）

### 🌐 网页素材捕捞（M0）：找参考不用再「另存为 → 翻文件夹 → 拖回来」
- **素材库新增「网页捕捞」**：打开内置捕捞窗，地址栏直接逛网页，**右键任意图片/视频一键捕捞进当前项目素材库**，还支持整页截图捕捞；主窗素材库实时回流，捞完即可连参考。
- **防盗链图也能捞**：下载走网页自身会话（cookie/Referer 原样），右键另存为存不下来的图这里能进库。
- 安全基线：捕捞窗按「默认拒绝」管权限，只放行 http(s) 导航——Nomi 第一块不可信内容面从第一天就锁死。

### 🎥 镜头节点一键选运镜（不搭 3D 场景）
- 视频镜头节点上直接**选 13 招运镜预设出运镜参考小片**（喂 Seedance 全能参考 video_ref）——想精确控制推拉摇移不再必须进 3D 导演台摆场景，轻量入口一键出。

### 🧍 假人姿势全面校准（13 个预设人眼级重修）
- **「垂头丧气」根治**：假人默认头颈前俯 18.8°（所有姿势都在低头看地）——基线抬回自然平视，站/走/跑/举手全员受益。
- **深蹲**：不再踮脚尖前趴「要栽倒」——全脚掌踩地、重心落回双脚支撑面、膝盖朝前不外劈。
- **半蹲**：不再「往后靠隐形墙」——前倾备战式、脚掌踩平。
- **单膝跪**：后小腿不再整条悬空——真正压在地上；跪膝不再内扣。
- **双膝跪**：不再塌成趴伏——直身跪，小腿/脚背贴地。
- **坐姿/行走/跑步**：手不再插进大腿、跑步不再埋头弓背。
- 底层新增**确定性姿势质量门**（重心-支撑面、部位触地、关节活动度、肢体穿插全量化）常驻测试套件——姿势质量从此可回归、不再靠肉眼撞运气。

### 🔗 画布协作收口（PR #33 系列，感谢 @1251912798）
- **文本节点连镜头真正生效**：连到镜头节点的文本内容会并入该镜头的生成上下文。
- 开发启动 fail-fast、暗色模式删除按钮等细节修复。

## v0.16.3 — 即梦/Agnes 视频修复 + Win 3D 全屏窗口栏（2026-07-08）

### 📹 即梦 CLI 视频不再「无限生成中」
- **即梦生成视频卡「仍在生成」8-15 分钟最后超时、积分已扣**的问题根治：即梦 CLI 在登录态失效 / 首次使用未授权 / 非会员被拒时会「静默失败」（退出码 0 或不吐任何原因），旧版把它当成还在排队干等。现在**提交后几秒内就报真实原因**——登录失效给重新登录指引、需网页端授权/会员的说清怎么办。
- **错误不再被误标成「模型未开通（火山方舟）」**：即梦的账号/会员类错误此前会被误分类、指引你去火山 Ark 控制台开模型（南辕北辙）。现在正确归为「账号权限不足」并给对的动作。
- 提示：即梦是**提交时扣积分**，被拦下的任务不会进生成历史；此类扣分请联系即梦客服申诉。部分视频模型**首次使用需在即梦网页端生成一次**完成内容安全授权，之后 Nomi 里即可正常用。

### 🖼️ Agnes 图生视频修复
- Agnes AI 图生视频一直失败的根因修复：首帧参数被打包成数组发给服务商（其后端要求单个链接）。档案声明的单图口径现在真正生效，无需再手动改包。

### 🖥️ Windows 3D 场景全屏找回窗口按钮
- Windows 下全屏 3D 场景此前盖住标题栏，最小化/最大化/关闭全不可点。现在顶部两行：窗口栏（logo + 窗口控制）+ 场景工具栏。感谢 @1251912798 的贡献（PR #33 部分合入，浏览器素材捕捞主体按重做规格另行推进）。

## v0.16.2 — 图生图「不按原图」根治（2026-07-06）

### 🖼️ 中转站图生图/参考图，老接入自动就位
- **不用再「删了重加」**：v0.16.0 起新接入的中转图片模型才有图生图，老接入的一直缺——现在启动时自动补齐（catalog v5 迁移）：参考图槽、图生图通道、比例 + 清晰度（1K/2K/4K）全部就位。云雾等 OpenAI 兼容中转的「参考图用不了 / 只能出 1K」到此为止。

### 📌 参考图永不过期
- **参考图一律优先用本地持久文件**，服务商临时直链（多数 1–3 天过期）只做兜底；发送时链接过期会**自动用本地文件重新上传换新链**。老项目重开后「参考图加载失败 / 服务商拉不到图」这一整类问题失效。

### 🛡️ 不再「静默出一张无关的图」
- 图生图/图生视频**缺参考图或该模型没有图生图通道时，在扣费之前直接报错**说明怎么办——此前会被静默当成纯文生图发出去：生成成功、扣费成功、结果和原图毫无关系（「图生图没按原图改」的主要来源）。
- 图生图模式没给参考图时生成按钮直接禁用并提示（对齐视频节点既有做法）；失效的参考图缩略图从含糊的「加载失败」改为「图已失效」+ 恢复提示。

## v0.16.1 — 3D 姿势重校准 + 本地 ComfyUI + 中转站管理（2026-07-05）

（另含：白模姿势预设重校准——坐/蹲/半蹲/跪/指向/举手等，front/side/q3/back/top 多视角回归，详见 docs/release-notes/v0.16.1.md。以下三节此前记为「未发布」，实际已随 v0.16.1 一并发出，此处归位。）

### 接入本地 ComfyUI（2026-07-04）

#### 🖥️ 用本机 ComfyUI 出图，无需 key、不花额度
- **模型设置里新增「本地 ComfyUI」接入卡**：在本机把 ComfyUI 跑起来（默认 `127.0.0.1:8188`），点「启用本地 ComfyUI」即可——无需任何 key，用你自己的 GPU 和模型出图，数据不出本地。默认关，不用的人零打扰。
- **启用时当场探测**：点启用会 `GET /system_stats` 检测本机 ComfyUI 是否在跑，连上就显示 Python 版本 / 显卡 / 显存（连不上也不阻断，可先启用再起服务，生成前不通会直接报错、不白跑）。接入地址可改（跑在别的端口/主机也行）。
- **内置一个「文生图」工作流**：启用后在生成画布直接选「本地 · 文生图」，填提示词 + 尺寸/步数/CFG/采样器/种子/checkpoint 名即可出图（用的是 ComfyUI 原生 `POST /prompt` → 轮询 `/history` → 取 `/view`，不经任何云端）。
- 底层是**通用「命名响应变换」钩子**：让任何形状古怪的上游响应（如 ComfyUI 动态 prompt_id 键 + 拼 /view URL）都能归一后走现有生成链路，未来接别的本地/自建后端可复用。

### 中转站模型「勾选启用」+ 供应商管理（2026-07-04）

#### 🔌 一堆拉取模型，勾选留哪几个
- **中转站模型可就地勾选启用/停用**：接一个中转站往往拉来几十上百个模型，此前只能在 chip 墙里逐个点 × 删（不可逆、删了要重拉）。现在模型设置里展开中转卡就是一份**可搜索、可勾选的清单**——搜模型名、按类分组、全选/全不选、实时「已启用 X/N」。
- **勾掉的模型立刻从生成模型下拉里消失**，想用再勾回来即可（可逆，清单一直在）；垃圾桶仍是彻底移除（要重拉才回来）。数据零迁移，卡副标题也从「N 个模型」变「X/N 已启用」。

#### 🔧 自定义/中转站供应商终于能管理了
- **一键删除整个供应商**：接错的、没用的中转 API，展开卡里「删除整个供应商」一键连模型带 key 全清（此前只能一个个删模型）。
- **改接入地址 / 换 key / 断开**：地址填错了（如阶跃星辰 StepFun 该用 `api.stepfun.com/v1`、Step Plan 订阅要用 `/step_plan/v1`）能就地改，不用删了重接；key 可更换、可断开。
- **阶跃星辰（StepFun）进官方厂商预设**：直接选名字、地址内置，不用手填也就不会填错。

### 3D 导演台运镜升级（2026-07-04）

#### 🎥 运镜像摄影师一样点出来
- **13 种运镜预设一键落轨迹**：选中相机就有「运镜预设」面板——推近/拉远/横移/升降/弧线/环绕，加上新的**变焦推/变焦拉/希区柯克变焦**（FOV 参与动画，机位后拉+变焦推的眩晕感）。设好时长/幅度点一下即生成，**连点自动串联多段**到时间轴，落完还能逐点精修。此前这 10 招只有 AI 能用，现在你自己也能点。
- **焦段滑杆（12–200mm）**：相机预览浮窗按摄影师心智调镜头——广角/标准/长焦，FOV 同步显示。
- **手持抖动**：0–100% 可调，播放与离屏出片逐帧一致，经参考视频真传进成片。
- **2.39:1 宽银幕**画幅。
- **一键导出运镜首尾帧**：按运镜段起点/终点各截一张落画布图片节点，接首尾帧生成工作流。

#### 🏙️ 一键搭场景
- **场景模板**：添加菜单「场景模板」——城市街道（马路/车道线/人行道/楼群/行道树/路灯/对向车）、室内房间（三面墙/床桌沙发/顶灯），追加进当前场景不清已有内容。
- **语义道具**：车辆/建筑/树木/路灯/墙面灰模，可调色/缩放/绑轨迹（车能沿路径开）。
- **AI 也能搭布景**：跟助手要「人物站在街道上」，站位参考图会自动铺上灰模街道/房间背景或单件道具（复用同一套布景，不再是悬空的孤零人物）；AI 运镜小片（如「相机推近街上的人」）同样可带布景，相机仍绕主体运镜。

#### 🛠 修复
- **运镜小片/站位图漏渲染**：此前离屏出片只渲染假人，你摆的几何体、灯和道具在 mp4 与定妆图里凭空消失——已根治（四个渲染点同源；真离屏出片抽帧验证道具确实进片）。
- **相机运镜录制存盘后朝向退化**：录好的相机运镜 take 存盘重载后，相机每帧朝向会退回静态注视点——序列化层漏存了瞄准轨迹，已修。

## v0.16.0 — 图片分镜 + 画布分镜体验大修（2026-07-02）

### 📸 图片分镜（拆镜头默认新模式）
- **拆镜头可选「图片分镜 / 视频分镜」**，默认图片：每镜一张静态画面（图生图），提示词只写构图/景别/光线/姿态，不再混入运镜、时长、台词。写小说配图、漫画分镜、先定画面再动起来，都用它。
- **镜卡按类型自适应**：图片镜头不显时长、模型清单切成图片模型；同一方案里可逐镜改类型。
- **图片镜头一键「转视频」**：画面满意后，节点右下角一键升成视频镜头——这张图自动作为首帧，镜号原样继承（先定画面 → 再动起来的 image-first 工作流打通）。

### 🎬 画布分镜看得懂了
- **「镜头 N」编号常显**：镜头生成出画面、被选中后编号不再消失，一屏分镜先后顺序一眼清。
- **参考卡不再吃镜号**：角色/场景/道具参考卡此前会占掉「镜头 1/2」，导致编号与方案错位——已根治。
- **画布助手能「整理画布」了**：跟助手说「帮我分一下/太乱了」，它真能动手把节点按镜号收成整齐网格（⌘Z 可撤销）。
- **镜卡参数收进一行**：比例/清晰度等参数不再折行占半张卡。

### 🔌 模型接入
- **第三方中转站按家拆卡**：接了多家中转不再糊成一张「其他模型」——每家一张卡，显示你接入时填的来源名称，哪个 key 对哪家一眼清。

## v0.15.0 — 画布修图 + 找素材 + 拆镜头闭环（2026-06-28）

### 🎨 画布上直接修图（对标 Lovart）
- **元素拆解**：生成的图当场把元素拆出来单独调（接入 Replicate，落到白板节点 + 专用工具条）。
- **改图里的文字**：对图中文字直接改，派生为新版本节点。
- **画布媒体粘贴**：可往画布直接粘贴图片 / 视频。

### 🔍 找素材面板
- 成片 / 参考**自动分组** + 缩略图识别（Phase 1，零模型即可用）。
- 接上文本大脑：读提示词把「没连卡的镜头」归到命名组，素材一多也秒找到。

### 🎬 拆镜头闭环
- Stage 1 实时编排 + Stage 2 半自动封顶接线：镜级 verify 原语 + loop 预算状态机，对账卡接内容偏差。

### 🧊 3D 生成 + RunningHub
- 新增**文生 3D / 图生 3D** 模型生成节点（混元 / Meshy 6 / HiTem3D v21）。
- 接入 **RunningHub** 供应商目录（可在模型设置填 key），补全 9 个视频 / 图片模型兼容集。

### 🧭 上手更顺
- 上手清单加体面退出：「不再提示」+ 满 2 天未完成自动永久关闭。
- 接模型失焦自动拉取、拉到的模型全部加入（不再只导认得的）。

### 🔧 关键修复
- **模型设置面板对未知 / model3d 类型不再整面白屏**（Issue #23 根因）。
- **提交幂等**：同一次意图提交 at-most-once，堵「丢回执 → 重试 → 二次扣费」。
- **查结果失败绝不重发付费任务**（治「平台冒出很多视频被扣费」）。
- 资产上传瞬态失败有界重试（治代理下多参参考图间歇 `127.0.0.1` 错）；素材库上传格式收口单一真相源（治音频静默蒸发）。

## v0.14.3 — 修复连线「没连上去」（2026-06-25）

### 🔧 连线锚点对齐到节点真实大小
- **现象**：画布上把节点连到镜头节点，线看着没接到节点上、飘在节点下方一点点的空中。
- **根因**：连线的落点一直按节点的「名义大小」算，但镜头节点（视频/图片）实际是**按画面比例渲染**的——名义高 340，按 16:9 实际只渲到 236 高。于是线的终点锚在 340 的一半处，比真实端口低了约 52px → 落在节点下方空中，看着「没连上」。
- **修复**：连线的起点 / 终点 / 拖拽预览线，全部改用节点**真实渲染大小**（与节点外壳同一套算法），名义大小不再参与。这类「线落在节点框外」的偏差从任何地方都不会再出现。

### 🔧 参考槽容量修复
- **现象**：给图生图节点点「+」加参考图，图加不进去；连线连进来也「连了等于没连」。
- **根因**：参考槽「还剩几格」算错了——负责显示/发送的逻辑把连线（含还没出图、只占位的连线）一起算进上限，但「要不要显示 +／能不能加」的逻辑只数了有图的和手动上传的，**漏算了那些占着位的「待生成连线」**。于是槽其实满了却以为还有空：`+` 照样显示、图也"存进去了"，可没有空位放它 → 加了不显示、发不出去；新连线落进满槽被丢弃。
- **修复**：「槽满没满」改为只由一个算法说了算（含占位的待生成连线）。槽真满时「+」正确隐藏、硬加给明确提示「参考已满（最多 N，含连线）」，不再默默丢。槽没满时一切照旧。

### 🔧 中转出图 404 修复
- **现象**：部分中转（如 code.newcli.com）接的图片模型一出图就报 `404`，地址长成 `.../codex/v1/v1/images/generations`——多了一段 `/v1`。
- **根因**：项目里有两套拼 URL 的逻辑悄悄走偏了。一套（接入测试用）会在「Base 末尾已带版本段、接口路径又以 `/v1/` 开头」时去掉多余的 `/v1`；但真正发图走的那套漏了这步去重，于是 Base `.../codex/v1` 拼上 `/v1/images/...` 就成了双 `/v1` → 中转找不到这个地址 → 404。
- **修复**：去重逻辑下沉到唯一的拼接器（生成和接入测试共用一套），删掉走偏的平行实现。这类「双版本段」错误从任何接口都不会再出现。

### 🎙️ 中转也能接配音了
- 之前火山原生配音要你**自己的火山 APP_ID / ACCESS_KEY**，没有就用不了。现在用你**已有的中转 key**（如 code.newcli.com）就能加 `seed-tts-2.0` 配音模型——走标准 OpenAI 兼容接口（`/v1/audio/speech`），出火山音色 mp3，即接即用。
- 原生火山配音（带情感控制）**保留并存**，两条路按各自能力出现，不互相替代。

自 v0.14.0 起的一轮体验打磨：画布节点设计统一、时间轴让音频成一等公民、3D 站位图姿势重新校准，并新增手绘「画板」节点。

### 🎨 画布 & 3D 站位图
- **3D 站位图能点开编辑器了**：之前只有右上角的小放大钮能点，整张图看着可点却没反应；现在空态点 body、已出图悬浮整图都能进 3D 编辑器。
- **坐 / 蹲 / 单膝跪姿势重新校准**：此前这几个地面姿势脚尖下垂、整体后仰、单膝跪整条后腿悬空。现在脚掌踩平、躯干立直、后膝落地——多视角逐个校准过。
- **节点 body 设计统一**：标题 / 启动器 / 底纹下沉成共享组件，各类节点不再各长一套。

### ⏱️ 时间轴 + 音频
- **音频成时间轴一等公民**：素材库音频可直接拖入 → 独立音频轨 + 预览试听 + 导出混音（顺带修掉导出静音的问题）。
- 底栏参数主次分层、空轨收起、叠加层收起条重设计——四条等高轨不再「又长又重」。

### 🖼️ 全新「画板」节点
- 新增手绘 / 草图画板画布节点，统一到与其他节点一致的共享外壳。

### 🔌 模型接入更顺
- 模型设置改「**已接入 / 可接入**」分层 + 分组折叠 + 自适应默认。
- **火山语音凭证拆成独立输入框**，不用再手拼 `APP_ID:ACCESS_KEY`。
- 聚合中转加「新手推荐」软标。

### 📦 素材 / 视频健壮性
- 素材库上传**放开音频 / 视频**（修 macOS 把 mp4 / mp3 灰掉）。
- **视频生成超时不再丢任务**：超时落「可找回」态，能重新拉取结果（重启也能拉）。
- 导入视频 + 时间轴播放中可拖动定位 + 入轨取真实时长（修「一律 5 秒」）。
- 参考图有序单源（修 @ 选不到连线参考图）+ 图加载失败给可读占位。

## v0.14.0 — 即梦会员直接出片 + 火山方舟 Seedance 视频（2026-06-24）

### 🎬 接入即梦官方 CLI：有即梦高级会员，用会员积分直接在 Nomi 出片
- **为什么**：AI 视频/图片最大的门槛是按次付费的 API 费。如果你已经是**即梦高级会员（maestro vip）**，现在可以直接用会员积分在 Nomi 里生成——零额外 API 费。
- **接什么**：即梦官方 `dreamina` CLI 的全部生成能力——
  - **视频**：文生视频 / 图生视频 / 首尾帧 / 全能参考（多模态）/ 多帧叙事，跑 Seedance 2.0（标准 / 快速 / VIP / Mini 变体）。
  - **图片**：文生图（即梦 3.0–5.0 共 8 个模型）/ 改图 / 图片超清。
- **怎么用**：模型设置 →「即梦会员」卡 → 扫码登录（抖音授权）→ 在生成画布选「即梦…（会员）」模型出片。
- ⚠️ **诚实门槛**：即梦 CLI 生成**仅高级会员（maestro vip）可用**——不是任意即梦会员，且光充积分不够，需开通高级会员档。接入卡会明示，非该档不让你点了干等。
- 技术上：全程本地命令行进程驱动，不碰逆向、不存你的密码（走官方设备码登录）。

### 🌋 接入火山方舟 Seedance 视频
- 新增火山方舟（Volcengine Ark）官方原生 Seedance 2.0 视频模型，带自己的方舟 API key 即可用。

### 🔧 体验修复
- 暗色模式低对比微调 + 品牌标识暗色下提亮。
- 画布编组描边/样式去重（单一真相源），徽章白底收口。

## v0.13.1 — 修复打包版「一键接入 Claude Code / Codex」连不上（2026-06-24）

### 🔌 一键接入：握手「连接已关闭」修掉
- **现象**：在 Nomi 点「一键接入 AI 编程助手」后，Claude Code / Codex 连不上（报 `Connection closed` / MCP 握手失败）。
- **根因**：写进客户端的启动命令指向一个**没被打进安装包**的脚本，且打包版不保证有 `node` 可用——进程一启动就退出，客户端在握手第一步就断开。
- **治本**：改成让 Nomi 用**自身程序**直接当 MCP 服务启动（不依赖外部 node、入口永远在包内）。Nomi 开着时实时同步、生成弹确认卡；没开时也能驱动。
- ⚠️ **升级后请重点一次「一键接入」**：旧配置不会被自动改写，重点一次即覆盖成正确入口（Claude Code 与 Codex 同理）。

## v0.13.0 — 新手引导旅途 + 外部 AI 桥修复（2026-06-24）

### 🎬 新手引导：一键看完「一句话 → 成片」
- **首页「看 Nomi 怎么出片」**：新人点一下，用内置示例片「修好一个小机器人」**预置回放**整条流水线——创作区逐字打字 → AI 拆分镜 → 一键铺成画布（真示例成片）→ 聚光讲解身份卡 / 3D 站位 / 运镜 → 切预览加字幕 / 导出 → 收尾接进你自己的创作。半自动节奏（看的自动播、讲工具的停下等你），随时可跳过；零额度、零网络。
- 旧的顶栏「带我去」逐步引导并入这条旅途；顶栏「上手」清单退化为被动进度，引导进行时自动让位。

### 🔌 外部 AI 桥（接 Claude Code / MCP）：修掉「静默黑洞」
- **根因**：外部 AI 驱动生成时，只有「app 正好开着那个目标项目」才弹得出付费确认卡；否则静默失败、零反馈——授权了却什么都没发生。
- **治本**：现在不管你在项目库首页还是开着别的项目，AI 要花钱都会**全局弹一张带项目名的确认卡**，确认才生成；切回 Nomi 项目列表自动刷新外部新建的项目。守住「令牌只有你亲手点确认才铸造」的安全底线。

### 🔧 体验修复
- **顶栏窄屏不再重叠**：标准宽度（≤1400px）下右侧库入口自动降级为纯图标，中间步骤器始终完整、左侧项目名截断，不再互相压字。
- 含 v0.12.1 起草未发布的**自动更新修复**（mac 补回 zip 产物 + 检测到新版给「前往下载」兜底）。

## v0.12.1 — 修复自动更新（2026-06-22）

修复旧版本「检查更新」报错、无法升级的问题。

### 🔧 自动更新
- **macOS 打包补回 `zip` 产物**：此前 mac 只打 `dmg`，`latest-mac.yml` 缺 zip，旧版本检查更新时报 `ZIP file not provided`、彻底卡死。补回 zip 后报错消除，Windows 端恢复就地自动更新。
- **macOS 手动更新兜底**：应用尚未做代码签名，而 macOS 的 Squirrel 强制校验签名才能就地装。现在 mac 检测到新版后给「前往下载」直达 GitHub 最新 release 手动重装，并明确标注原因，不再静默失败。

## v0.12.0 — 导演工作台（2026-06-22）

自 v0.10.13 起积累的一次大版本：Nomi 从「写故事 → 生成 → 剪片」的流水线，长成了一个能**把你脑子里的画面精确传达给 AI** 的导演工作台——3D 摆构图、运镜控节奏、一键被编程助手驱动。

### 🎬 3D 导演台 + AI 运镜（全新支柱）
- **3D 导演台**：在 3D 场景里摆角色姿势、架机位、设画幅，截机位画面当生成参考——文字说不清的构图直接「搭」出来，锁住跨镜头的空间关系。内置 12 个校准过的预设动作，姿势后自动落地（不悬空）。
- **AI 站位参考**：让 AI 按你的描述自动搭好站位 / 动作 / 朝向 / 机位，离屏出图、连成参考，锁站位锁身份。
- **AI 运镜**：对话里描述运镜 → 沿相机轨迹渲一段运镜小片 → 喂进 Seedance 2.0 全能参考（video_ref），把运镜复刻到真实视频上。轨迹编辑器已挂进 3D 编辑器，人也能手搓。

### 🔗 一键接入编程助手（能力核）
- Nomi 的创作能力抽成无头能力核（CLI / MCP）：在「模型接入」面板一键把 Nomi 接进 **Claude Code / Codex / Cursor**，零配置写好接入，用对话指挥建项目、加节点、连参考、触发生成。
- MCP 驱动 Nomi 所见即所得；Nomi 关着时也能在 Claude 里用 elicitation 确认付费生成，不死锁。
- 握手回显客户端协议版本，兼容所有 MCP 客户端。

### 🤖 模型接入更省心
- 模型接入页简化：分区列表 + 顶部能力概览，一眼看清「已通 / 半通 / 断」。
- 免配置上传做成多床冗余链（litterbox → tmpfiles），运镜 mp4 喂入彻底不用 key。

### 🎞️ 时间轴重做
- clip 用真实帧缩略图，一眼辨识。
- 重做（⇧⌘Z）、trim 帧气泡、一键拼片、选中 clip 就地「重生成」。
- 播放器补音量 / 静音 / 全屏 / 逐帧。

### ⚡ 性能 + 稳定性
- 冷启动 + 流式 + 画布全链路卡顿三处根治；窗口不再被代理探测 / 能力核阻塞。
- 损坏项目优雅降级——缺字段不再让项目硬锁死打不开。
- 提示词库缓存落盘。

### 🛡️ 设计系统 + 字体
- 用户可见面全面对齐设计系统 v2；重接被切断的设计 token + 新增悬空 token 门岗。
- 字体统一到 Inter Variable（自托管品牌字体），根治界面字体不一致。

完整提交历史见 `git log v0.10.13..v0.12.0`。
