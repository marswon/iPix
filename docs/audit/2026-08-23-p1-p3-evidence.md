# P1→P3 MCP 可编辑单镜生成阶段证据

日期：2026-08-23  
分支：`codex/video-generation-parameter-research-20260823`
范围：P1/P2 边界、P3 零 provider-quota seam、真实 MCP/GUI 走查，以及一次受控的 APIMart 低规格图片 transport smoke。

## 用户价值结论

- P1 解决“换模型/供应商/输入模式就要学习一套 Nomi 私有格式”的摩擦：模块、provider、model、mode、参数和素材都是声明数据；不支持时在 provider 前明确失败。
- P2 解决“我只是改了一个参数/换了参考图，却不知道系统实际提交了什么”的摩擦：candidate 每次编辑都生成新 revision/hash；字段被丢弃时有 warning/ledger；封存后编辑返回 `new_draft_required`，不会偷偷改原任务。
- P3 seam 解决“要离开 MCP 去 Nomi 点第二次、崩溃后怕重复扣费”的摩擦：provider-neutral adapter 只接受 sealed contract；envelope 先落盘；provider 已接受但回执丢失时只进入 reconcile，不盲重提。
- 能力降级修正了一个过度保守的阻塞：供应商缺少原生幂等/取消能力时，仍可正常完成一次明确提交；缺失能力只改变异常路径的人话和可承诺范围。APIMart 现在是 `observe_only`，不是被错误挡在生成门外。

## 用户体验验收原则

- MCP 客户端优先：客户端能提供主进程可验证的 attestation 时，用户在当前客户端点一次即可由主进程签发 receipt；没有可验证证明时只回退到同一个 challenge 的一张 Nomi 卡，避免双重审批。
- 可编辑和通用：模型、供应商、image/video/audio 模式、参数和参考素材均是 candidate 数据，不写死在 dispatcher；所有编辑都在封存前发生，封存后通过新草稿保护旧合同。
- 简洁可见：用户只看到决定所需的摘要和一个 primary action；进行中用文字+图标+进度，错误写明事实与唯一下一步；内部状态名不直接甩给用户。
- 真实场景：后续 journey 必须覆盖自由换模型/供应商/模式/参数/参考素材，以及取消、重启、断线、回执丢失和 provider 能力缺失，而非只覆盖固定 fixture 的 happy path。

## 已通过的代码证据

| 能力 | 证据 |
|---|---|
| Module/Provider/Model capability schema | `moduleManifest.test.ts`、`moduleRegistry.test.ts`：两个不同 profile，未知 model/mode fail-closed |
| Runtime/Run binding | `productionExecutionBinding.test.ts`、`productionRunReducer.test.ts`：foreign Run、provider namespace、idempotency mismatch 在 command boundary 拒绝 |
| Asset identity/lease | `projectAssetStore.test.ts`、`projectAssetLease.test.ts`：写入 ID 与列表 ID 相同；foreign/expired/content mismatch 拒绝 |
| Editable contract | `executionContract.test.ts`：provider/model/mode/parameter/reference add/remove/replace/reorder 改动都会产生新 hash；unsupported field 可解释 |
| Shared semantic seam | `mcpGenerationDispatcher.test.ts`：MCP/GUI 通过同一个 `generationPlanning` callback；未调用 `runTask`/gateway |
| Semantic tool vocabulary + durable draft owner | `mcpGenerationTools.test.ts`、`productionGenerationOperationStore.test.ts`：33 工具目录中的 11 个语义入口走同一 handler；`operation → edit → seal → restart read` 通过 `ProductionRun` events/snapshot/CAS，不另造草稿事实源 |
| Provider-neutral adapter | `generationRuntimeAdapter.test.ts`、`generationProviderCapabilities.test.ts`：full/observe-only/submit-only 共用同一 adapter；只有缺少可执行 submit 才阻塞 |
| Durable envelope/recovery | `productionRunRuntimeEnvelope.test.ts`、`productionRunResume.test.ts`、`generationSingleShot.test.ts`：重启可读、seal conflict、provider acceptance ordering、unknown/reconcile-only |
| Run-owned single-shot submit | `productionGenerationSubmission.test.ts`：sealed contract → durable job/approval/budget/envelope → intent claim → fake provider submit exactly once；receipt 丢失进入 `submission_unknown`，重启只返回 reconcile；明确 crash-before-dispatch 才能恢复；新尝试必须创建新 attempt/contractHash/receipt |
| 能力降级用户投影 | `generationRecoveryProjection.test.ts`、`mcpToolResults.test.ts`、`productionNotifications.test.ts`、`nomiMcpApps.test.ts`：MCP 文本、系统通知和内嵌 widget 共用“核对/停止等待/新尝试”人话，不显示自动重试承诺 |
| Read integrity | `productionRunRepository.test.ts`：坏 snapshot/stale snapshot 只内存重建；半写 JSONL 返回 `migration_parse_error`，不静默截断 |

## 真实 MCP/UI 零额度证据

命令：

```bash
pnpm run test:mcp
```

结果：

- J-MCP1 PASS：45 assertions、10 steps、约 0.9s。
- 6 次 loopback mock vendor request；真实 provider quota = 0。
- 生成 image×2 + video×1；结果资产可从磁盘重新打开；progress、image block、deep link 均可见。
- headless journey 的 App dialog = 0；第一次 elicitation 后的后续生成没有第二次确认。

共享确认入口还做了真实 Electron + MCP 走查：`node tests/ux/spend-elicit-app-open.walk.mjs`，22 assertions、8 次 mock vendor request、真实 quota = 0。客户端不能提供可验证 attestation 时，Nomi 只出现一张简短确认卡（显示项目、模型、产物和一次“确认生成”）；客户端能提供 attestation 时，确认只出现在当前 MCP 客户端，Nomi 侧没有第二张卡。人眼检查了真实截图：
`tests/ux/shots/spend-elicit-app-open/02-fallback-card-shown.png`、`03-during-elicit-no-card.png`、`04-after-generate.png`。

这条现有旅程验证的是 legacy canvas generation 的真实 MCP 运输与客户端确认链，不能冒充 P3 `generation.single-shot` 已接入默认 ProductionRun；后者仍需完成下方待做项。

新增的 `nomiMcpGenerationPlanning.test.ts` 已用真实 MCP JSON-RPC `initialize → tools/call(create) → tools/call(edit) → tools/call(preview)` 跑通一个零额度 fixture：草稿写入 `generation.single-shot` Run，重启读取仍保留 revision/模式/参考素材，`runTask` 调用数为 0。它证明了真实 transport 到 Run owner 的 planning/edit 路径，不证明 provider submit 或真人 gate 已开。

新增的 `mcpSemanticGenerationConfirmation.test.ts` 已跑通 `tools/call(create) → preview → request_generation_gate`：同一 challenge 在当前 MCP 客户端完成一次 elicitation，并提交可验证 attestation 供主进程签发 receipt，随后自动 `decide → start`；没有调用 legacy `runTask` 或 gateway。客户端无法提供可验证 attestation 时，协议只允许同一个 challenge 走 GUI fallback。

新增的 `moduleCatalogBootstrap.test.ts` 与 `nomiMcpGenerationPlanning.test.ts` 还验证了默认语义 registry 从用户模型目录派生 provider/model/mode/参数声明；同一真实 MCP JSON-RPC 旅程可以依次切换供应商、模型、image/video 模式、参数和参考素材，revision/hash 每次更新，且 `runTask` 始终为 0。目录声明不等于 provider 已具备原生幂等/核账能力，能力目前刻意保持 fail-closed。

最终零额度交付复核：`pnpm run gates` 通过（690 test files、6115 tests；lint 0 errors/95 existing warnings；typecheck/build 通过）；`pnpm run test:mcp` 通过（45 assertions、6 mock vendor requests、真实 quota = 0）；`node tests/ux/spend-elicit-app-open.walk.mjs` 通过（22 assertions、8 mock vendor requests、真实 quota = 0）。打包 MCP smoke 也通过（33 tools、24 resources、claude/codex/cursor 三个签名来源）。

## APIMart 受控 smoke（2026-08-23）

- 凭证通过 Nomi 的模型目录写入本机安全存储；没有写入代码、日志或提交，也没有在输出中回显明文。
- 只跑一张 `Seedream 5.0 Pro`：`text-to-image`、`1:1`、`1K`、`n=1`；没有视频或高分辨率请求。
- `POST /v1/images/generations` 返回 HTTP 200 和 `data[0].task_id`；`GET /v1/tasks/{task_id}` 从 `pending` 到 `completed`；结果为 1024×1024 JPEG，结果地址 HTTP 200。
- 这证明凭证、模型参数和 APIMart 的异步查询传输可用，但**不证明可以安全自动重试或保证远端取消**：官方文档只明确提交与 task 查询，没有给出原生 idempotency key 或取消接口。按照能力降级方案，APIMart 作为 `observe_only` provider 正常允许一次提交；未知回执只核账，不自动重提。
- 为了让实测使用与产品网络出口一致，修正了 transport spike 漏传 Electron `session.defaultSession` 的测试入口；该修复不触碰生产生成路径。

## 当前未完成（已移出 P3 零额度闭环）

1. provider 的真实取消接口仍按供应商能力逐项接入；没有能力时保持“停止等待/供应商核对”，不伪造成功。
2. 多镜连续性、Timeline adopt、动态模块和 Agent parity 属于 P4+，不阻塞本轮单镜交付。
3. 真实 provider 付费 smoke 仍是唯一决策门：需要用户决定凭证、具体模型和预算范围；在此之前不向真实供应商提交。

## 继续推进记录（2026-08-24）

- `generationRuntimeAdapter` 新增统一 `query`/`reconcile` seam；provider 没有对应能力时返回结构化 observation unsupported，不触发 submit。
- runtime envelope 新增 `lastPoll { status, raw, observedAt }`；ProductionJob 记录 `providerStatus`、`lastPollAt` 和状态变化时间，查询结果可在重启后复读。
- Run-owned submission 新增 `poll`：只凭已经持久化的 providerTaskId 查询，processing 状态进入 `polling`，失败状态进入 `needs_attention`，不会自动重新提交；MCP stdio / app RPC 的 `reconcile(found)` 默认复用这条路径。
- 新增验证：runtime adapter query、envelope poll restart、submission poll exactly-once（30 个 focused tests 通过）；provider degradation E2E 仍为 8 checks / zero credit / zero network。

## 继续推进记录（2026-08-24，读取与恢复边界）

- `ProductionRunService.readProjection` 与 `listFull` 现在都保持纯读：读取运行列表不会隐式触发恢复扫描、状态改写或旧 driver 重踢；恢复必须由显式 `resumeUnfinishedRuns`/语义提交 owner 调用。
- 旧 playbook 恢复扫描识别 `generation.single-shot` Run 后跳过 legacy `submission_unknown` 改写；语义任务继续由 `ProductionGenerationSubmission.resume/poll/reconcile` 按 providerTaskId、runtime envelope 和 reconcile-only 规则处理，避免把单镜任务误交给旧生成链路。
- 新增回归：listFull 零 repository.execute；semantic single-shot restart recovery 零 legacy mutation；服务/driver/shot-gate focused 19 tests、typecheck 通过。

## 继续推进记录（2026-08-24，Run 写入 fencing）

- `ProductionRunRepository.execute` 现在在读取最新事件、revision CAS、追加事件/命令索引和写 snapshot 的整个同步临界区持有独立的短期 repository mutex；不同进程不能同时读同一 revision 后各自 append。真正需要跨重启拒绝旧 owner 的 durable fencing 仍由 semantic `run.lock` 提供。
- 现有 semantic submission 的 `run.lock` 仍负责 provider dispatch/outbox；repository mutex 独立存在，避免嵌套锁，同时让 legacy service、MCP、GUI、semantic submission 共用同一 append 保护。
- 新增回归：外部 worker 持有 repository lock 时，第二个 repository 以 `run_lock_busy` fail closed；repository / semantic submission focused 24 tests、typecheck 通过。
- 变更后的全量回归：`pnpm run test` 通过（699 test files、1 skipped；6181 tests、1 skipped）；build、filesize、tokens、i18n、archetype-sources、diff-check 通过。

## 继续推进记录（2026-08-24，结果落地与 Artifact）

- `GenerationProvider` 新增可选的 provider-owned `materialize`：供应商只负责把自己的任务响应解析成一个已验证 output descriptor（image/video/audio + URL），Runtime Adapter 不猜字段；没有该能力仍允许提交与查询，不会被错误阻塞。
- APIMart 适配器按其已验证的 `data.result.images/videos/audios` 形状解析结果；没有新增真实付费请求。默认 app RPC/stdio 的 `reconcile(found)` 在 terminal poll 后自动尝试一次 materialization，失败时返回人工核对，而不是把远端完成冒充本地文件。
- Asset store 新增确定性 materialization sink：`providerTaskId + providerOutputId/URL` 形成稳定路径，重试不会产生重复 `-2` 文件；下载经过 `hardenedFetch` 的协议、私网、大小和媒体类型校验，data URL 走受限字节路径。
- Run 在本地 receipt 写入后才追加 `artifact.add`，再把 job 置为 `ready`、runtime envelope 置为 `materialized`；中途崩溃可按同一 artifact/receipt 重放。新增 materializer、APIMart shape、Asset retry、job observation 及 submission artifact 回归。
- 结果落地 focused suites 通过（含 provider 无 materialize 时仍可提交、但不伪造 Artifact）；上一轮全量 `pnpm run test` 通过（700 test files、1 skipped；6188 tests、1 skipped）；build、filesize、tokens、i18n、archetype-sources、diff-check 通过。

## 继续推进记录（2026-08-24，真实语义 GUI 兜底）

- 桌面 RPC 与 in-Electron stdio 现在都显式使用同一份运行时 generation policy：默认关闭；`NOMI_MCP_GENERATION_SINGLE_SHOT_V1=1` 只打开零额度 E0 规划/预览；再显式设置 `NOMI_MCP_GENERATION_SINGLE_SHOT_E1_V1=1` 才打开单镜提交。两个开关分开，避免打开规划能力时意外打开付费提交。
- `session/open` 的当前项目 bootstrap 会一次发行规划所需的 scope；来源（Codex/Claude/Cursor）从已验证 transport 透传到 `ProductionRun`，并把本次候选的 provider/model 写入 Run policy。用户点一次确认后不再因来源或策略丢失而被拒绝。
- 新增真实 Electron + MCP stdio 旅程：`node tests/ux/mcp-generation-single-shot-gui-fallback.e2e.mjs`，14 assertions，使用隔离项目和 loopback APIMart fixture，真实 provider POST=1、查询 GET=1、真实额度=0。客户端不提供 elicitation 时，Nomi 只显示一张确认卡；卡片展示 provider/model、镜头摘要、参考图数量、费用边界和“忽略/确认生成”，点击确认后原 MCP 请求继续，随后 `nomi_reconcile_generation(found)` 生成可持久化 Artifact。
- 人眼检查截图：`tests/ux/shots/mcp-generation-single-shot-gui-fallback/01-semantic-gate.png`、`02-semantic-complete.png`。确认卡的主操作只有一个，背景被正确压暗；完成后回到正常创作界面，没有第二张审批卡或重复提交。

## 本轮最终验证（2026-08-24）

- `tests/ux/mcp-generation-provider-degradation.e2e.mjs`：8 assertions，覆盖 observe-only / submit-only、换 provider/model/mode/parameters/references、已知 task 查询、未知回执、停止等待；zero credit、zero network provider calls。
- `pnpm run test:mcp`：45 assertions、6 次 loopback mock vendor requests、真实 provider quota = 0。
- `pnpm run gates`：694 test files passed、1 skipped；6137 tests passed、1 skipped；lint 0 errors/95 existing warnings；typecheck/build 通过；所有额外 controls/site/secret/symlink 门通过。

## 全门证据

```bash
pnpm run gates
```

结果：694 test files passed、1 skipped；6137 tests passed、1 skipped；lint 0 errors/95 existing warnings；typecheck/build passed；`.claude/.gates-ok` 已生成。

## 唯一决策门

P1/P2/P3 零额度 semantic journey、真实 MCP/GUI 共享确认走查、打包 smoke、结果 Artifact/materialization、重启/核账和 provider 能力降级均已有证据。能力降级原则已由用户确认：供应商没有这些能力时不因此禁用一次明确提交；`submission_unknown` 只核账不盲重提，无法证明远端取消时只停止 Nomi 等待。

现在只剩一个不可代替用户决定的动作：是否开启真实 provider 付费 smoke（选择凭证、模型和预算上限）。没有这个决定，工程交付已经完整停在“零额度可验收”边界，不会擅自消费真实额度。
